name: Build Android APK

on:
  push:
    branches: [main]
    paths-ignore:
      - "release/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Restore green school logo
        run: |
          mkdir -p app/src/main/res/drawable
          base64 --decode branding/school-logo.base64 > app/src/main/res/drawable/school_logo.png

      - name: Build APK
        run: gradle assembleDebug

      - name: Prepare APK
        run: |
          mkdir -p release
          cp app/build/outputs/apk/debug/app-debug.apk release/Elenga-Multimedia-School.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Elenga-Multimedia-School-APK
          path: release/Elenga-Multimedia-School.apk
          if-no-files-found: error
          retention-days: 30
